{
  "name": "kaynak-newsroom",
  "private": true,
  "description": "Newsroom project management tool for journalists",
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"wait-on tcp:3000 && npm run dev:client\"",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "build": "cd client && npm install && npm run build",
    "start": "npm run start --prefix server",
    "install:all": "npm install && npm install --prefix server && npm install --prefix client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "wait-on": "^8.0.1"
  },
  "engines": {
    "node": ">=18"
  }
}
