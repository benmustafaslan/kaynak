import type { Story } from '../../types/story';

interface WorkflowMetricsProps {
  stories: Story[];
}

/** Workflow metrics for stories. Overdue was removed when deadlines moved to pieces. */
export function WorkflowMetrics(_props: WorkflowMetricsProps) {
  return null;
}
