/**
 * Tiptap script editor with lock, version control, and auto-save.
 * To be implemented: toolbar (Bold, Italic, H1, H2, List, Link, Fact-Check),
 * lock acquisition/release, "Save as new version", auto-save every 10s.
 */
export function ScriptEditorPlaceholder() {
  return (
    <div className="rounded border border-stone-200 bg-stone-50 p-4 text-sm text-stone-500">
      Script editor (Tiptap) placeholder. Lock + version control + fact-check button to be wired.
    </div>
  );
}
